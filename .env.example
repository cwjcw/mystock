# 外部域名与 RSS 路径前缀（示例/占位）
PUBLIC_DOMAIN=stock.your_domain.cn
# 使用用户名作为前缀：username；或指定固定前缀，如 rss
RSS_PREFIX=username

# 仅存哈希、Token 只显示一次（true/false）
RSS_TOKEN_HASH_ONLY=true

# 限速：每窗口内允许的请求次数、窗口秒数（默认 1/60）
RSS_RATE_LIMIT=1
RSS_RATE_WINDOW=60

# Web 服务端口与调试开关
APP_PORT=18888
DEBUG=false

# Flask 会话密钥（请使用强随机值替换示例）
SECRET_KEY=change-me-please

# 作者只爬了1次，IP就被封了，只好去买了个代理，以下是代理配置示例，若无需代理可删除或留空（我使用的是快代理的私密代理，一年费用人民币50元）
# 用这个链接注册快代理，作者赚点中介费 https://www.kuaidaili.com/?ref=kd97m8wdouwj
PROXY_API_URL=https://dps.kdlapi.com/api/getdps/?secret_id=YOUR_SECRET_ID&signature=YOUR_SIGNATURE&num=1&sep=1
PROXY_USERNAME=your_proxy_username
PROXY_PASSWORD=your_proxy_password
PROXY_REFRESH_INTERVAL=900
BULK_WORKERS=20
